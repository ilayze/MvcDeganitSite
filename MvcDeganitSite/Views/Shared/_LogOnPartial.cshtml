@if(Request.IsAuthenticated) {
    <text>שלום <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("יציאה", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("הרשמה", "LogOn", "Account") ]
}
